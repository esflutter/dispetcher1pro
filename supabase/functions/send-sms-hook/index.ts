// =====================================================================
// УСТАРЕВШАЯ КОПИЯ — НЕ ДЕПЛОИТЬ.
//
// Канонический исходник: dispetcher1/supabase/functions/send-sms-hook/index.ts
// Эта копия отставала от канона (маппинг ошибок RedSMS, лимиты).
// Удалить каталог не дала политика прав — файл заменён заглушкой, чтобы
// случайный деплой отсюда сразу упал, а не вернул старую логику.
// =====================================================================
Deno.serve(() =>
  new Response(
    JSON.stringify({
      error: "stale_copy",
      message:
        "Эта копия send-sms-hook устарела. Деплойте из supabase/functions/send-sms-hook.",
    }),
    { status: 410, headers: { "Content-Type": "application/json" } },
  ));
