<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Сеть нужна для Supabase (БД, Storage, Auth). В debug-манифесте она
         декларируется отдельно для нужд Flutter tool, но release-сборка
         читает только этот файл — без явного разрешения на проде
         все сетевые вызовы молча падают. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <application
        android:label="Диспетчер №1 PRO"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Deep link для возврата из браузерной формы YooKassa.
                 Когда юзер нажимает «Вернуться на сайт», YooKassa
                 редиректит на dispatcher1pro://payment/result?id=...
                 Android видит зарегистрированную схему и открывает
                 это Activity, передаёт URL в Flutter через app_links. -->
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="dispatcher1pro"/>
            </intent-filter>
            <!-- Android App Links: после публикации release-билда (см.
                 .well-known/assetlinks.json на jokaynapesbem.beget.app)
                 Android Verifier проверит SHA-256 ключа подписи и при
                 успехе запрос https://jokaynapesbem.beget.app/payment/return*
                 будет открывать приложение НАПРЯМУЮ, без выбора браузера.
                 Сейчас есть только промежуточная HTML-страница; этот
                 intent-filter готовит поведение «open in app» к моменту,
                 когда APP_LINKS_SHA256 будет установлен на VPS.
                 autoVerify="true" — Google проверит assetlinks.json
                 при первой установке приложения.

                 ВАЖНО: до того как файл assetlinks.json будет реально
                 отдаваться на /.well-known/assetlinks.json (см.
                 supabase/functions/assetlinks/index.ts), App Links не
                 верифицируется и поведение остаётся как сейчас —
                 запрос идёт в браузер на промежуточную страницу. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="https"
                      android:host="jokaynapesbem.beget.app"
                      android:pathPrefix="/payment/return"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- На Android 11+ launchUrl(tel:) и подобное возвращают false без
             объявленной видимости пакетов в <queries>. Без этих интентов
             кнопки «Позвонить заказчику», «Открыть в Я.Картах»,
             «Открыть карту», «Отправить email» молча не работают на
             свежих устройствах. -->
        <intent>
            <action android:name="android.intent.action.DIAL"/>
            <data android:scheme="tel"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="tel"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="mailto"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
        <!-- geo: — стандартная Android-схема для адресов на карте.
             Все картографические приложения (Яндекс Карты, Google Maps,
             2ГИС, MAPS.ME и др.) объявляют intent-filter на эту схему,
             поэтому при нажатии на адрес система сама показывает
             chooser «Открыть с помощью». -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="geo"/>
        </intent>
    </queries>
</manifest>
